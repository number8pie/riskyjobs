#NEXT COMMIT MESSAGE:

Added code to search .php to limit the results (current setting at time of writing is 5 results per page) for pagination.
Added pagination to the search results if the $num_pages variable is more than 1.